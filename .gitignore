# Node / React
node_modules/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env

# .NET / C#
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
appsettings.Development.json

# IDE / System
.vscode/
.vs/
.idea/
.DS_Store
Thumbs.db

# Docker (optional)
# .dockerignore handles docker contexts, but we can ignore data folders here if needed
backend-aspnet/data/database.sqlite
# But wait, if they want to submit the DB with seeded data, they might need the sqlite file.
# I will NOT ignore the sqlite file so the teacher can run it directly.
